[project]
name = "agentforge-studio"
version = "0.1.0"
description = "AgentForge UIManager scripting panel UI"
requires-python = ">=3.11"
dependencies = [
    "agentforge-core",
]

[tool.uv.sources]
agentforge-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
