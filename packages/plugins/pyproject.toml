[project]
name = "agentforge-plugins"
version = "0.1.0"
description = "AgentForge Plugins Loader and Registry"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
