[project]
name = "agentforge-analyzers"
version = "0.1.0"
description = "AgentForge pluggable media analyzers (motion, color, text, audio)"
requires-python = ">=3.11"
dependencies = [
    "agentforge-core",
    "agentforge-sdk",
]

[tool.uv.sources]
agentforge-core = { workspace = true }
agentforge-sdk = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
