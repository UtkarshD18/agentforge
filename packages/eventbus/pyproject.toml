[project]
name = "agentforge-eventbus"
version = "0.1.0"
description = "AgentForge Event Bus Broker"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
