[project]
name = "agentforge-core"
version = "0.1.0"
description = "AgentForge Core Library (EventBus, Scheduler, Models, Storage)"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
    "cryptography>=42.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
