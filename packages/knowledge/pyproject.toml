[project]
name = "agentforge-knowledge"
version = "0.1.0"
description = "AgentForge Semantic Knowledge & Memory Retrieval Engine"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
